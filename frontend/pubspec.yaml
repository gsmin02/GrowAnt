name: growant
description: "GrowAnt — AI와 투자 대결하는 모의투자 앱"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  # TODO(market-slice): 추가 예정 — flutter_riverpod ^2.5, dio ^5.4 (+dev: http_mock_adapter).
  #   마켓 REST 슬라이스 표준 데이터 계층. 스펙: docs/superpowers/specs/2026-06-07-market-rest-slice-design.md §8

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
